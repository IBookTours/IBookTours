# ============================================
# IBOOKTOURS - Environment Variables Template
# ============================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control!

# ============================================
# REQUIRED FOR PRODUCTION
# ============================================

# NextAuth Secret - REQUIRED
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
NEXTAUTH_SECRET=

# Your production URL (change for production)
NEXTAUTH_URL=http://localhost:3000

# Demo Mode - ONLY set to 'true' for local development/testing
# For production, leave unset or set to 'false'
# DEMO_MODE=true

# Demo account credentials (required when DEMO_MODE=true)
# SECURITY: These are NOT hardcoded - you must set these explicitly
# DEMO_USER_EMAIL=demo@ibooktours.com
# DEMO_USER_PASSWORD=your-secure-demo-password
# DEMO_ADMIN_EMAIL=admin@ibooktours.com
# DEMO_ADMIN_PASSWORD=your-secure-admin-password

# Site URL for SEO, metadata, and password reset emails
# IMPORTANT: Set this to your production domain!
# Password reset emails will have broken links if this is wrong.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ============================================
# GOOGLE OAUTH (Optional - demo login still works)
# ============================================
# Get these from https://console.cloud.google.com/
# 1. Create a new project
# 2. Enable Google+ API (or Google Identity)
# 3. Go to Credentials > Create OAuth Client ID > Web Application
# 4. Add authorized redirect URIs:
#    - Development: http://localhost:3000/api/auth/callback/google
#    - Production: https://your-domain.com/api/auth/callback/google
# 5. Copy Client ID and Client Secret below
# Cost: $0 (Free)
# Note: Account linking is enabled - users can sign up with email,
#       then later link their Google account seamlessly.

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ============================================
# STRIPE (Optional - runs in demo mode if not set)
# ============================================
# Get these from https://dashboard.stripe.com/
# Use test mode keys for development
# Cost: $0 (Test mode)

STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# ============================================
# NEON DATABASE (Required for user management & bookings)
# ============================================
# Get connection string from https://console.neon.tech/
# 1. Create a new project
# 2. Copy the connection string from the dashboard
# Cost: $0 (Free tier - 0.5GB storage)

DATABASE_URL=

# ============================================
# BREVO EMAIL (Optional - runs in demo mode if not set)
# ============================================
# Get API key from https://app.brevo.com/
# Cost: $0 (Free tier - 300 emails/day)

BREVO_API_KEY=
BREVO_SENDER_EMAIL=noreply@ibooktours.com
BREVO_SENDER_NAME=IBookTours
BREVO_NEWSLETTER_LIST_ID=1
CONTACT_EMAIL=contact@ibooktours.com

# ============================================
# CMS PROVIDER (Optional - defaults to mock/static data)
# ============================================
# Options: 'mock' (default), 'payload', 'sanity'
# When set to 'payload', configure PAYLOAD_API_URL below
# CMS_PROVIDER=payload

# Payload CMS - https://payloadcms.com/
# PAYLOAD_API_URL=https://your-payload-instance.com/api

# ============================================
# RATE LIMITING (Optional - uses in-memory if not set)
# ============================================
# For production with multiple instances, use Upstash Redis
# Get free tier at https://upstash.com (10,000 requests/day free)
# UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-token

# Customize rate limits for API endpoints
# RATE_LIMIT_AUTH_MAX=10          # Max login attempts per 15 min
# RATE_LIMIT_CONTACT_MAX=5        # Max contact form submissions per 15 min
# RATE_LIMIT_NEWSLETTER_MAX=3     # Max newsletter signups per hour
# RATE_LIMIT_PAYMENT_MAX=10       # Max payment attempts per minute

# ============================================
# GOOGLE ANALYTICS 4 (Optional - no tracking if not set)
# ============================================
# Get Measurement ID from https://analytics.google.com/
# 1. Create a GA4 property (or use existing)
# 2. Go to Admin > Data Streams > Web
# 3. Create a data stream for your domain
# 4. Copy the Measurement ID (starts with G-)
# Cost: $0 (Free tier includes unlimited events)
#
# Features tracked:
# - E-commerce funnel (view_item, add_to_cart, purchase)
# - Search and filter usage
# - Page views with SPA navigation support
# - Lead generation (contact, newsletter)
#
# Privacy: Events only fire AFTER user grants analytics consent.
# Uses GA4 Consent Mode v2 for GDPR compliance.

NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Enable debug mode to see events in GA4 DebugView (development only)
# NEXT_PUBLIC_GA_DEBUG=true

# ============================================
# FEATURE TOGGLES (Optional - all enabled by default)
# ============================================
# Set to 'false' to disable a feature site-wide.
# When disabled:
# - Navigation links are hidden
# - Pages redirect to /tours gracefully (no 404/403)
# - Homepage sections are hidden
#
# Use these to temporarily disable features you can't offer.

# Core Products
# NEXT_PUBLIC_FEATURE_CAR_RENTAL=true
# NEXT_PUBLIC_FEATURE_HOTELS=true
# NEXT_PUBLIC_FEATURE_VACATION_PACKAGES=true
# NEXT_PUBLIC_FEATURE_DAY_TOURS=true
# NEXT_PUBLIC_FEATURE_BLOG=true

# Travel Services Section (compact icon grid)
# NEXT_PUBLIC_FEATURE_TRAVEL_SERVICES=true
# NEXT_PUBLIC_FEATURE_EVENTS=true
# NEXT_PUBLIC_FEATURE_AIRPORT_TRANSFERS=true
# NEXT_PUBLIC_FEATURE_LOCAL_GUIDES=true

# ============================================
# LOGGING (Optional)
# ============================================
# Log level: debug, info, warn, error
# LOG_LEVEL=info
